---
description: Deprecated compatibility alias for /cs-review
---

This is a 2.x compatibility alias, not a separate workflow stage. Read
`.claude/commands/cs-review.md` completely and follow it exactly. Report the
canonical next step as `/cs-review`; do not recommend `/cs-recheck` again.
