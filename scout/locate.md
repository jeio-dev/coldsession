<!-- coldsession-scout template: locate version: 1 params: symbol -->
Task: locate where `{{symbol}}` is defined and where its behavior is implemented in this repository.

- Cite the definition first, then the smallest set of places that implement its behavior.
- Status `answered` requires at least one evidence item.
- If you cannot find it, use status `not_found` and list every pattern you searched, with its paths, in `searched`.
