interface:
  display_name: "Coldsession Review"
  short_description: "Review independently, then revise when needed"
  default_prompt: "Use $cs-review to independently review the current phase revision and automatically revise it when findings require changes."
