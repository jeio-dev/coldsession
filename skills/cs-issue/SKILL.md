---
name: cs-issue
description: Create one GitHub backlog issue for a small change without approving or implementing it. Use when the user invokes $cs-issue or asks coldsession to capture a change as an issue.
---

# Coldsession Issue

Read `.agents/coldsession/commands/cs-issue.md` completely and follow its
workflow from the repository root. Treat accompanying text and `--draft` as
`$ARGUMENTS`. Translate `/cs-plan` to `$cs-plan` when reporting a next step
to a Codex user.
